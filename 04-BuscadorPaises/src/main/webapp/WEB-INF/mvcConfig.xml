<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd">

	<!-- Hace lo mismo que annotation config, es preferible en el mvc usar esta -->
	<mvc:annotation-driven />
	<context:component-scan
		base-package="controller" />

	<!-- registramos el objeto que se encarga de la navegación hacia las vistas -->
	<bean id="resolver"
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/"></property>
		<property name="suffix" value=".jsp"></property>
	</bean>

	<!-- Navegaciones estáticas -->
	<mvc:view-controller path="toInicio" view-name="Inicio" />
	<!-- Esta linea lleva a la pagina inicio si no se especifica direccion -->
	<mvc:view-controller path="/" view-name="Inicio"/>
	<mvc:resources mapping="/static/**" location="/static/" />
	<mvc:resources mapping="/resources/**" location="/" />
</beans>
